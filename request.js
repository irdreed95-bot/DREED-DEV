const requestClient=supabase.createClient(window.SUPABASE_URL,window.SUPABASE_KEY);const requestForm=document.querySelector("#projectForm"),notice=document.querySelector("#loginNotice");(async()=>{const {data:{user}}=await requestClient.auth.getUser();if(!user){notice.innerHTML='تريد تتابع طلبك؟ <a href="auth.html">سجّل دخولك أو أنشئ حساب</a>.';return}notice.textContent="أنت مسجل دخول. بعد الإرسال يظهر الطلب مباشرة داخل «طلباتي».";const email=user.email||"";requestForm.addEventListener("submit",async e=>{e.preventDefault();const fd=new FormData(requestForm),btn=requestForm.querySelector("button");btn.disabled=true;btn.textContent="جارٍ إرسال الطلب…";const {error}=await requestClient.from("project_requests").insert({user_id:user.id,name:String(fd.get("name")||user.user_metadata?.full_name||"عميل"),email,project_type:String(fd.get("type")||"مشروع"),message:String(fd.get("message")||"")});if(error){notice.textContent="تعذر حفظ الطلب: "+error.message;btn.disabled=false;btn.textContent="إرسال الطلب ↗";return}notice.textContent="تم إرسال طلبك بنجاح. تقدر تتابع حالته من صفحة طلباتي.";requestForm.reset();btn.textContent="تم إرسال الطلب ✓";setTimeout(()=>location.href="account.html",900)})})();